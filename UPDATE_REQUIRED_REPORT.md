# 업데이트 필요 내역

- 작성일: 2026-07-13
- 대상: `childinfo-dashboard`
- 기준: 프로덕션 빌드, 데이터 가드, npm 의존성 감사, 주요 보안 설정 및 코드 검토
- 현재 판정: **빌드는 가능하지만, 보안 보완 전에는 운영 출시를 권장하지 않음**

## 1. 확인 결과 요약

| 영역 | 상태 | 주요 내용 |
| --- | --- | --- |
| 데이터 검사 | 통과 | 데이터 파일 308개, 인프라 파일 302개, 복지 항목 60개 확인 |
| 프로덕션 빌드 | 통과 | Vite에서 2,966개 모듈 빌드 성공 |
| 번들 크기 | 개선 필요 | 단일 JavaScript 번들이 약 2.64 MB(gzip 약 546 KB) |
| 자동 테스트 | 미구성 | 단위·통합·E2E 테스트, lint, type-check 명령 없음 |
| 의존성 보안 | 위험 | `npm audit` 기준 총 38건(Critical 2, High 16, Moderate 6, Low 14) |
| 관리자 인증 | 위험 | 관리자 PIN 설정과 인증이 브라우저 상태에 의존 |
| 개인정보 저장 | 위험 | 아동 생년월일·성장·체온·수유·접종 기록이 `localStorage`에 저장됨 |
| Supabase RLS | 양호 | 사용자별 상담 접근 및 관리자 JWT 판정 정책이 정의됨 |
| HTTP 보안 헤더 | 양호 | CSP, 클릭재킹 방지, MIME sniffing 방지 등이 설정됨 |

## 2. P0 — 출시 전에 반드시 처리

### 2.1 관리자 인증을 서버 기반으로 전환

현재 문제:

- `src/App.jsx`에서 로컬 관리자 PIN이 없으면 해당 브라우저의 사용자가 직접 PIN을 만들고 관리자 UI에 진입할 수 있음
- 로그인 실패 횟수와 잠금 상태가 React 메모리에만 있어 새로고침으로 초기화 가능
- 브라우저의 `isAdmin` 값은 권한 판정 수단으로 신뢰할 수 없음

필요 작업:

- Supabase Auth 로그인 또는 별도의 서버 인증을 관리자 진입의 필수 조건으로 사용
- `app_metadata.role = admin` 등 서버가 발급한 클레임만 관리자 권한으로 인정
- 관리자 전용 조회·등록·삭제는 UI 상태와 무관하게 RLS 또는 서버 API에서 검증
- 클라이언트의 “최초 관리자 PIN 설정” 흐름 제거
- 로그인 실패 제한이 필요하면 서버 측에서 사용자·IP·시간 기준으로 적용

완료 조건:

- 새 브라우저나 조작된 `localStorage`만으로 관리자 화면 또는 관리자 데이터에 접근할 수 없음
- 일반/익명 사용자의 관리자 쿼리가 서버에서 거부됨
- 관리자 계정의 정상 조회·답변·삭제 흐름에 대한 테스트가 존재함

### 2.2 고정 AES 키와 로컬 관리자 데이터 제거

현재 문제:

- `src/utils/security.js`에 AES 키가 하드코딩되어 있어 배포 번들에서 누구나 추출 가능
- 같은 키로 암호화된 `localStorage` 값은 사용자가 복호화하거나 변조할 수 있음
- 이 구조는 보안 저장소가 아니라 난독화에 가까움

필요 작업:

- 클라이언트 번들에 포함된 공통 비밀키 제거
- 관리자 인증 정보는 Supabase Auth 등 서버 관리 인증으로 이전
- 오프라인 개인정보 암호화가 반드시 필요하면 사용자 비밀번호에서 KDF로 키를 유도하고, salt·버전·복구 정책을 별도로 설계
- 보안 요구가 명확하지 않다면 암호화 기능처럼 보이는 현재 래퍼를 제거하고 저장 위험을 사용자에게 명시

완료 조건:

- 소스와 프로덕션 번들에 고정 암호화 비밀키가 없음
- 브라우저 저장값 변조로 관리자 권한을 얻을 수 없음

### 2.3 아동 건강정보 저장 정책 확정

현재 문제:

- 생년월일, 성별, 키, 몸무게, 체온, 수유, 예방접종 및 발달 기록이 장기간 `localStorage`에 평문 저장됨
- 공유 기기, 브라우저 프로필 탈취, XSS 발생 시 정보가 노출될 수 있음

필요 작업:

- 저장 항목, 목적, 보유 기간, 삭제 방식과 동의 문구 정의
- 불필요한 개인정보는 저장하지 않거나 세션 종료 시 삭제
- 로컬 저장을 유지할 경우 전체 데이터 삭제 기능과 명확한 안내 제공
- 서버 동기화를 도입할 경우 사용자별 접근 제어, 전송 암호화, 보유 기간 및 탈퇴 시 삭제 구현
- 오류 로그나 분석 도구에 아동정보가 포함되지 않는지 확인

완료 조건:

- 개인정보 처리방침과 실제 저장 동작이 일치함
- 사용자가 저장 여부를 이해하고 전체 데이터를 삭제할 수 있음
- 저장 만료 또는 삭제 정책이 자동 테스트로 검증됨

### 2.4 노출 가능성이 있는 API 키 회전

현재 문제:

- 로컬 환경에는 `VITE_*` 형식의 외부 API 키가 존재하며, 이 접두사는 브라우저 번들 노출을 전제로 함
- `vite.config.js`도 서버용 환경변수가 없을 때 `VITE_*` 키를 대체값으로 허용함

필요 작업:

- 현재 사용 중인 외부 API 키를 발급 기관에서 회전
- 운영 키는 `BW_API_KEY`, `NURSING_API_KEY` 같은 서버 전용 환경변수로만 저장
- 서비스 코드는 `/api/child-facilities`, `/api/nursing-rooms` 프록시만 호출
- `VITE_BW_API_KEY`, `VITE_NURSING_API_KEY` 대체 경로 제거
- Vercel 및 Apps in Toss 배포 산출물에 키 문자열이 없는지 검사

완료 조건:

- 프로덕션 JS, source map, Git 이력 및 배포 로그에 운영 API 키가 없음
- 폐기한 기존 키로 외부 API를 호출할 수 없음

## 3. P1 — 빠른 시일 내 처리

### 3.1 npm 취약 의존성 업데이트

검사 결과:

- Critical: 2건
- High: 16건
- Moderate: 6건
- Low: 14건
- 합계: 38건

주요 항목:

- `@fastify/middie`: 인증·미들웨어 경로 우회 관련 Critical/High 권고
- `shell-quote`: 명령 삽입 관련 Critical 권고
- `vite@5.4.21`: Windows 경로 우회 및 개발 서버 파일 노출 관련 권고
- `postcss@8.5.9`: CSS stringify XSS 관련 권고
- `ws`, `protobufjs`, `tmp`, `js-yaml` 등 전이 의존성 취약점
- 다수는 `@apps-in-toss/web-framework` 및 Granite 도구 체인에서 유입되며 일부는 자동 수정 불가

필요 작업:

1. 별도 브랜치에서 `@apps-in-toss/web-framework`의 지원되는 최신 안정 버전과 마이그레이션 문서 확인
2. PostCSS를 취약 범위 밖 버전으로 업데이트
3. Vite 메이저 업데이트 호환성 검증
4. 직접 업데이트가 불가능한 전이 의존성은 상위 패키지 릴리스 또는 공식 완화책 확인
5. 업데이트 후 `npm audit`, 프로덕션 빌드, Toss/Granite 빌드와 실제 앱 동작 재검증

주의:

- `npm audit fix --force`를 바로 적용하지 말 것. 메이저 버전 변경과 Apps in Toss 호환성 파손 가능성이 있음
- 개발 서버는 업데이트 전까지 신뢰할 수 있는 로컬 네트워크에서만 실행하고 외부에 노출하지 않는 것을 권장

완료 조건:

- Critical 0건, High 0건을 목표로 함
- 해결 불가능한 항목은 런타임 노출 여부, 완화 조치, 상위 패키지 이슈 링크와 재검토 일자를 기록

### 3.2 자동 검증 체계 추가

현재 문제:

- `package.json`에 단위 테스트, 통합 테스트, E2E, lint, type-check 스크립트가 없음
- 현재 자동 검증은 데이터 가드와 Vite 빌드뿐임

필요 작업:

- ESLint 및 React Hooks 규칙 추가
- Vitest와 Testing Library로 핵심 유틸리티·서비스 테스트 추가
- Playwright 등으로 핵심 사용자 흐름 E2E 테스트 추가
- CI에서 최소한 `guard:data`, lint, test, build, audit 실행

우선 테스트 대상:

- 나이/개월 수 및 성장 백분위 계산
- 급여·시설·지역 필터링
- 수유·체온·성장 기록 추가/수정/삭제
- Supabase 일반 사용자와 관리자 권한 분리
- 상담 메시지 2,000자 제한 및 사용자별 접근
- 개인정보 전체 삭제 및 보유 기간 만료

완료 조건:

- 모든 검증 명령이 `package.json`에 정의됨
- CI에서 실패 시 배포가 중단됨

### 3.3 API 프록시 남용 방지

현재 상태:

- GET 메서드 제한, 정수 범위 제한, 10초 타임아웃과 캐시가 적용되어 있음
- 인증이나 호출량 제한은 없음

필요 작업:

- Vercel 또는 별도 API 계층에서 rate limit 적용
- 허용 Origin/Host 정책 검토
- 외부 API 응답 크기 제한 및 예상 콘텐츠 타입 검증
- 업스트림 오류 본문을 그대로 전달할 필요가 있는지 검토
- 요청량과 오류율 모니터링 추가

완료 조건:

- 반복 호출로 외부 API 쿼터나 서버리스 비용을 쉽게 소진할 수 없음
- 비정상 콘텐츠 타입과 과도한 응답 크기가 차단됨

### 3.4 Supabase 운영 설정 검증

SQL 파일의 정책은 적절한 방향이지만 실제 운영 DB에 적용됐는지는 별도 확인이 필요함.

필요 작업:

- 운영 DB에서 RLS 활성화 여부와 현재 정책 목록 확인
- 익명 로그인 활성화 범위 확인
- 관리자 이메일 하드코딩 대신 `app_metadata.role` 중심으로 통일 검토
- `pg_cron` 14일 삭제 작업이 실제 예약되었는지 확인
- Realtime이 RLS 정책을 동일하게 따르는지 일반/관리자 계정으로 검증
- 상담 데이터 삭제에 필요한 감사 로그와 운영 절차 검토

완료 조건:

- 익명 사용자 A가 사용자 B의 상담을 조회·구독·삭제할 수 없음
- 일반 사용자가 관리자 답변 타입을 삽입할 수 없음
- 관리자만 전체 상담을 조회·구독할 수 있음
- 14일 초과 데이터가 예정대로 삭제됨

## 4. P2 — 품질 및 성능 개선

### 4.1 JavaScript 번들 분할

현재 결과:

- JavaScript 약 2.64 MB
- gzip 약 546 KB
- Vite의 1 MB 청크 경고 발생

필요 작업:

- 탭, 관리자 화면, 차트, 지도 기능을 `React.lazy`와 동적 import로 분리
- Recharts, Framer Motion, Supabase 등 대형 의존성의 실제 사용 범위 확인
- `manualChunks`는 측정 결과에 따라 적용
- 초기 화면에 필요하지 않은 전국 데이터의 지연 로딩 검토
- Lighthouse 또는 Web Vitals 기준으로 전후 결과 비교

완료 조건:

- 초기 JS 다운로드와 실행 시간이 개선됨
- 저사양 모바일 환경에서 첫 화면과 탭 전환 성능이 허용 기준을 충족함

### 4.2 보안 헤더 현대화

현재 상태:

- CSP, `X-Frame-Options`, `X-Content-Type-Options`, Referrer Policy가 설정됨
- `X-XSS-Protection`은 최신 브라우저에서 폐기된 헤더임

필요 작업:

- 실제 배포 응답에서 헤더가 적용되는지 확인
- CSP 위반 보고 수집 방안 검토
- `Permissions-Policy` 추가 및 위치정보 등 필요한 기능만 허용
- HTTPS 전용 운영이 확정되면 HSTS 적용 검토
- CSP의 `style-src 'unsafe-inline'` 제거 가능성 검토

### 4.3 외부 링크 및 HTTPS 정리

- 코드에 남은 `http://` 외부 링크를 HTTPS 지원 여부에 따라 교체
- 새 창 링크에는 필요한 `rel="noopener noreferrer"` 적용 여부 확인
- 외부 위치 서비스에 위도·경도가 전달된다는 점을 사용자에게 안내

## 5. 권장 실행 순서

1. 관리자 권한을 Supabase Auth/RLS 기반으로 전환
2. 고정 AES 키와 로컬 관리자 PIN 제거
3. 아동정보 저장·삭제·동의 정책 확정
4. 외부 API 키 회전 및 서버 전용 환경변수로 통일
5. Apps in Toss, Vite, PostCSS 및 전이 의존성 업데이트
6. 테스트·lint·CI 추가
7. API rate limit과 Supabase 운영 정책 검증
8. 번들 분할 및 모바일 성능 측정
9. 실제 배포 환경의 헤더·CSP·키 노출 최종 검사

## 6. 재검증 명령

```powershell
npm.cmd run guard:data
npm.cmd run build
npm.cmd audit
```

테스트와 lint 도입 후에는 다음과 같은 단일 검증 명령을 추가하는 것을 권장함.

```json
{
  "scripts": {
    "check": "npm run guard:data && npm run lint && npm test && npm run build"
  }
}
```

## 7. 릴리스 승인 체크리스트

- [ ] 관리자 권한이 서버에서만 판정된다.
- [ ] 클라이언트 번들에 고정 비밀키나 운영 API 키가 없다.
- [ ] 아동정보 저장과 삭제 정책이 구현되고 고지되었다.
- [ ] Critical/High 의존성 취약점이 제거되거나 공식 완화 조치가 문서화되었다.
- [ ] 테스트, lint, 데이터 검사와 빌드가 CI에서 통과한다.
- [ ] 일반 사용자와 관리자 RLS 시나리오를 실제 운영 설정에서 검증했다.
- [ ] API 호출량 제한과 오류 모니터링이 적용되었다.
- [ ] 실제 배포 응답의 CSP 및 보안 헤더를 확인했다.
- [ ] 프로덕션 산출물에서 비밀정보 문자열 검사를 완료했다.
- [ ] 모바일 초기 로딩과 주요 사용자 흐름을 실기기에서 확인했다.

---

이 문서는 2026-07-13 시점의 로컬 소스와 설치된 의존성을 기준으로 작성되었으며, 수정 적용 후 빌드·감사·권한 테스트를 다시 수행해야 한다.
